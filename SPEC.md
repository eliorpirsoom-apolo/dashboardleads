# SPEC — דשבורד / CRM למשרד דיגיטל

> מקור אמת לפיתוח. כל שינוי מהותי באפיון מתעדכן כאן לפני שנוגעים בקוד.
> עדכון אחרון: 2026-07-13

## 1. מהות המערכת

CRM דו-צדדי:

- **צד משרד (ADMIN)** — ניהול כלל הלקוחות, משימות, פגישות, מסמכים, והזרמת דאטה.
- **צד לקוח (CLIENT)** — הלקוח מנהל את הפעילות הדיגיטלית שלו: לידים, קמפיינים, דוחות, משימות, מסמכים. סוכני מכירות של הלקוח הם משתמשים נוספים תחת אותו חשבון לקוח.

## 2. עקרונות ארכיטקטורה ("תשתית חכמה")

1. **ליבה אחת + מודולים.** כל סוגי הלקוחות חולקים ליבה משותפת (לידים, סטטוסים, משימות, מסמכים). ההבדלים בין סוגי הלקוחות הם מודולים שנדלקים לפי `Client.type`:
   - `general` — ליבה בלבד.
   - `realestate` — ליבה + מודול פרויקטים/מלאי/חוזים.
   - `seo` — ליבה + מודול נתוני SEO (קונסול/אנליטיקס).
2. **תצורה במקום קוד.** סטטוסים, צבעים ושדות חופשיים מוגדרים ע"י הלקוח כנתונים (DB/JSON) — לא כקוד. מרחיבים את התבנית הקיימת של `leadFields.json`.
3. **הפרדת צדדים במשימות.** לכל משימה שדה "בעלות" — משרד או לקוח.
4. **ערוצי הודעות כשכבה מופשטת.** תזכורות והודעות עוברות דרך מנגנון אחד; V1 מייל בלבד, SMS/וואטסאפ מתווספים כערוץ נוסף בלי לשנות את הליבה.

## 3. החלטות שהתקבלו (סגור)

| נושא | החלטה |
|---|---|
| טיפול בלידים | הלקוח מטפל. מגדיר סטטוסים וצבעים בעצמו — חלק מהמערכת |
| סביבת הרצה | Vercel בשלב הראשון; בהמשך מעבר ל-VPS. בנייה ניידת — בלי תלות בשירותים ייחודיים ל-Vercel |
| אחסון קבצים | שירות תואם S3 (עובד מ-Vercel והיום ומה-VPS בעתיד ללא שינוי). גישה בקישורים חתומים. בחירת השירות — שאלה פתוחה 2 |
| הסכם חתום | העלאת PDF חתום (ללא חתימה דיגיטלית במערכת) |
| קליטת לידים | קליטה ישירה ל-endpoint אחיד. מקורות: פייסבוק לידים, טפסי אלמנטור (וורדפרס), שיחות פייקול — חלקם דרך Make/Zapier. הסקרייפר מ"ליד מנג'ר" — legacy, לא מפתחים עליו |
| עדכון מלאי דירות | סוכן המכירות של הלקוח, כמשתמש במערכת של הלקוח |
| תפקידים | ADMIN (משרד) / CLIENT (לקוח + סוכניו). אין תפקיד שלישי |

## 4. תפקידים והרשאות

- **ADMIN** — רואה ומנהל הכול: כל הלקוחות, משתמשים, מסמכים, משימות של שני הצדדים.
- **CLIENT** — סקופ מלא ללקוח אחד בלבד (`User.clientId`). כמה משתמשים לאותו לקוח (בעלים, סוכני מכירות).
- התחברות: אימייל + סיסמה, וכן כניסה עם Google (OAuth).

## 5. מודל נתונים (כיוון — מעל הסכמה הקיימת)

קיים ונשאר: `User`, `Client`, `Campaign`, `Lead` (בהתאמות), `SyncLog`.

תוספות מתוכננות:

| מודל | תפקיד |
|---|---|
| `Client.type` | `general` \| `realestate` \| `seo` — קובע מודולים |
| `LeadStatus` | סטטוס פר-לקוח: שם, צבע, סדר, `systemKind` (`new`/`in_progress`/`won`/`lost`) — `won` מפעיל אוטומציית מלאי בנדל"ן |
| `CustomFieldDef` | הגדרת שדות חופשיים פר-לקוח (שם, סוג); הערכים ב-`Lead.data` (JSON, תבנית קיימת) |
| `LeadNote` | הערות על ליד: מי כתב, מתי, טקסט |
| `LeadSource` | נקודת קליטה ישירה: טוקן API פר-לקוח/קמפיין, ערוץ, פלטפורמה |
| `Task` | משימה/פגישה (`type`): כותרת, מועד, בעלות (משרד/לקוח), משויך למשתמש, תזכורת (ערוץ+מועד), קישור אופציונלי לליד |
| `Document` | קובץ: קטגוריה (הסכם / חשבונית / קבלת פייסבוק / קבלת גוגל / אחר), חודש (לקבלות), URL באחסון |
| `Budget` | תקציב פר-לקוח/קמפיין: סכום, תקופה (חודשי/שבועי) → עלות לליד מחושבת |
| `Project` (נדל"ן) | שם, לוגו פרויקט, שיוך ללקוח |
| `UnitType` (נדל"ן) | טיפוס דירה בפרויקט: שם, מחיר, תוכנית דירה (קובץ), כמות כוללת |
| `Contract` (נדל"ן) | חוזה: ליד, טיפוס דירה, ערך, PDF חתום |

**שדות ליד (ליבה):** מספר ליד, קמפיין, קהל, מודעה, ערוץ הגעה, פלטפורמה, תאריך, שם, טלפון, מייל, סטטוס, הסכמה לדיוור, הערות, שדות חופשיים.
**לידים טלפוניים (עתידי — פייקול):** `Lead.kind = call` + מספר, הקלטה, משך שיחה. המודל מוכן, האינטגרציה בשלב 3.

**אוטומציית מלאי:** ליד עובר לסטטוס עם `systemKind=won` ומקושר לטיפוס דירה ⟵ המלאי הזמין של הטיפוס יורד. ביטול הסטטוס ⟵ המלאי חוזר.

## 6. שלבי פיתוח

### שלב 1 — ליבת CRM
- התחברות (אימייל+סיסמה, Google), תפקידים והפרדת סקופ מלאה.
- ניהול לקוחות (ADMIN): יצירה, סוג לקוח, לוגו, משתמשים.
- לידים: טבלה, שדות ליבה, סטטוסים+צבעים בהגדרת הלקוח, שדות חופשיים, הערות, הסכמה לדיוור.
- קליטה ישירה: endpoint עם טוקן פר-לקוח (בסיס ל-webhooks של פייסבוק/דפי נחיתה).
- משימות + פגישות + לוח שנה, בעלות משרד/לקוח, תזכורות במייל.
- מסמכים: העלאה ידנית לפי קטגוריה וחודש; הלקוח רואה, המשרד מעלה.

### שלב 2 — מודול נדל"ן
- פרויקטים: שם, לוגו, תוכניות דירה, טיפוסים ומלאי, מחירים מתעדכנים.
- אוטומציית מלאי (סעיף 5), בקשות רכישה, חוזים + ערך חוזים.
- תקציב חודשי/שבועי ועלות לליד; דוחות פר-פרויקט.

### שלב 3 — הודעות ואינטגרציות
- ערוצי SMS/וואטסאפ לתזכורות; הודעות אוטומטיות ללקוחות וסוכנים.
- פייקול (לידים טלפוניים), ספירת שיחות וואטסאפ ממנהל המודעות.
- העלאת קבלות אוטומטית מתיקייה ייעודית; "2 המודעות החזקות של החודש" אוטומטית (עד אז — סימון ידני).
- דוח הודעות תפוצה.

### שלב 4 — מודול SEO
- חיבור Search Console + Analytics (OAuth), גרפים: תנועה, מיקומים, לידים.
- בדיקת מיקומים ב-AI — נבחן כשיהיה כלי בשל.

## 7. שאלות פתוחות (לא חוסמות את שלב 1, נסגרות לפני השלב הרלוונטי)

1. **בחירת DB מנוהל** — המלצה לאישור: PostgreSQL מנוהל (Neon או Vercel Postgres). עובד מ-Vercel היום; במעבר ל-VPS מתקינים PostgreSQL על השרת ומעבירים נתונים ב-dump/restore. נדרש לצעד 0.2.
2. **בחירת שירות אחסון קבצים** — המלצה לאישור: Cloudflare R2 (תואם S3, זול, ללא עלות תעבורה). נדרש לצעד 1.12; לא חוסם את תחילת חלק א׳.
3. **ספק SMS/וואטסאפ** — יש חשבון קיים (019 / Twilio / WhatsApp Business)? נדרש בשלב 3.

נסגרו ב-13.07.2026: מקורות קליטה (פייסבוק לידים, אלמנטור, פייקול — חלקם דרך Make/Zapier); סביבת הרצה (Vercel תחילה ⟵ VPS בהמשך) — ראו טבלת ההחלטות בסעיף 3.

## 8. סטטוס מימוש — הושלם (13.07.2026)

**כל שלבי הפיתוח (0–4 + רוחבי) מומשו.** מה שנותר: החיבורים החיצוניים בלבד — ראו `CONNECTIONS.md`.

- סכמה מלאה (30 מודלים) ב-`prisma/schema.prisma` — PostgreSQL בפרודקשן, מראת SQLite לפיתוח (`scripts/gen-sqlite-schema.mjs`).
- שדות דינמיים פר-לקוח: `CustomFieldDef` + בונה שדות ב-UI (החליף את leadFields.json).
- הסקרייפר של "ליד מנג'ר" הוסר (שמור בהיסטוריית גיט). הדאטה הישן: `prisma/legacy-leadmanager.db` + `npm run import:legacy`.
- UI דו-צדדי מלא: `/admin` (משרד) ו-`/app` (לקוח), עברית RTL, ערכה כהה.
- בדיקות עשן לזרימות הקריטיות: `npm run test:smoke` (13 בדיקות — אימות, סקופינג, קליטה+דה-דופ, אוטומציית מלאי, תפוצה+הסכמה, תזכורות).
