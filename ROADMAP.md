# 🗺️ מפת דרכים לפרודקשן מלא + מדריך התמצאות

> נכתב 15.07.2026. מיועד לכל מי שמצטרף לפרויקט — איפה הדברים עומדים בדיוק,
> ומה נשאר עד פרודקשן מלא. מסמכי העומק: [SPEC.md](SPEC.md) (האפיון ומודל הנתונים),
> [CONNECTIONS.md](CONNECTIONS.md) (מדריך חיבורים מפורט), [README.md](README.md) (פיתוח מקומי).

---

## 1) תמונת מצב — מה חי היום

**המערכת בנויה במלואה ופרוסה בפרודקשן.** כל שלבי הפיתוח מהאפיון (0–4 + הקשחה)
הושלמו ונבדקו. מה שנשאר הוא חיבורים חיצוניים והפעלה תפעולית — לא פיתוח.

| רכיב | מצב | פרטים |
|---|---|---|
| קוד המערכת (כל המודולים) | ✅ הושלם | ליבת CRM, נדל"ן, הודעות, SEO — נבדק ב-13 בדיקות עשן |
| פרודקשן | ✅ חי | https://dashboard-leads-apollo13.vercel.app |
| אירוח | ✅ | Vercel, פרויקט `dashboard-leads`, צוות APOLLO (`apollo13`) |
| בסיס נתונים | ✅ | Neon Postgres (אינטגרציית Vercel), מיגרציות הוחלו |
| פריסה אוטומטית | ✅ | push לענף `claude/busy-ptolemy-6u3vvd` ⟵ פריסה לפרודקשן |
| חשבון מנהל | ✅ | נוצר דרך `/setup` (העמוד נעול מעתה) |
| אחסון קבצים (R2) | ✅ | bucket‏ `crm-files`, נבדק קצה-לקצה, משתנים ב-Vercel |
| סודות | ✅ | `AUTH_SECRET`, `CRON_SECRET`, `RECEIPTS_UPLOAD_TOKEN` מוגדרים |
| Cron | ⚠️ חלקי | תזכורות פעם ביום 05:00 בלבד (מגבלת תוכנית חינמית) — ראו שלב 1.3 |
| מייל (SMTP) | ❌ טרם | הודעות נרשמות ביומן כ"ממתינה לחיבור ספק" |
| כניסת Google | ❌ טרם | כפתור מוסתר עד הגדרת OAuth |
| קליטת לידים אמיתית | ❌ טרם | ה-endpoint חי; אין עדיין חיבורי Make/Zapier/אלמנטור אמיתיים |
| SEO / Meta / SMS / פייקול | ❌ טרם | לפי הצורך פר-לקוח (שלב 3) |

**עיקרון חשוב שהמערכת בנויה עליו:** כל ערוץ שלא חובר עדיין לא שובר כלום —
הפעולה נרשמת ביומן ("ממתינה לחיבור ספק") ונשלחת רגיל ברגע שהספק מחובר.

---

## 2) איך הפרויקט בנוי — מפה מהירה למי שנכנס לקוד

- **שני צדדים:** `/admin` (המשרד — רואה הכול) ו-`/app` (לקוח — רואה רק את עצמו).
  ההרשאה נאכפת בשרת בכל בקשה דרך `scopeClientId` ב-[src/lib/api.ts](src/lib/api.ts).
- **ליבה + מודולים:** `Client.type` (general / realestate / seo) קובע אילו מסכים
  ומודולים דולקים — [src/lib/modules.ts](src/lib/modules.ts). להוסיף מודול = לא לגעת בליבה.
- **קליטת לידים:** endpoint ציבורי אחד [src/app/api/intake/[token]/route.ts](src/app/api/intake/%5Btoken%5D/route.ts)
  עם טוקן פר-מקור, זיהוי שדות סלחני (כולל עברית), דה-דופ לפי טלפון מנורמל, ויומן קליטה.
- **הוקים עסקיים:** [src/lib/hooks.ts](src/lib/hooks.ts) — אוטומציית מלאי נדל"ן
  (סטטוס "עסקה" מוריד דירה מהמלאי, ביטול מחזיר, עם יומן ביקורת) + אוטומציות הודעות.
- **שכבת הודעות:** [src/lib/messaging.ts](src/lib/messaging.ts) — מייל/SMS/וואטסאפ
  דרך נקודה אחת, הכול נרשם בטבלת `Message`.
- **אחסון:** [src/lib/storage.ts](src/lib/storage.ts) — R2 בפרודקשן, דיסק מקומי בפיתוח.
  הורדות תמיד דרך `/api/files/[id]` שבודק הרשאות.

## 3) סביבת פיתוח מקומית — התחלה ב-4 פקודות

```bash
npm install
npm run db:dev      # יוצר DB מקומי (SQLite) + Prisma client
npm run db:seed     # נתוני דמו — משתמשים: admin@agency.local / admin123
npm run dev         # http://localhost:3000
```

בדיקות עשן (מול שרת רץ עם seed): `npm run test:smoke -- http://localhost:3000`

### ⚠️ גוצ'ות שחובה להכיר לפני שנוגעים בקוד

1. **סכמה כפולה:** `prisma/schema.prisma` הוא מקור האמת (Postgres).
   `prisma/schema.sqlite.prisma` נוצר אוטומטית לפיתוח — **לא לערוך אותו ידנית**.
   בגלל זה אסור להשתמש בסכמה ב-enums / Json / ‎@db.*‎ — רק טיפוסים ניידים.
2. **מיגרציות — כללי המשחק השתנו מאז העלייה לאוויר:** עד ההשקה עבדנו עם מיגרציית
   init יחידה שמתחדשת (`npm run db:migration:init`). **מעכשיו אסור להריץ אותה** —
   ה-DB בפרודקשן כבר קיים. שינוי סכמה חדש = מיגרציה אינקרמנטלית חדשה בתיקיית
   `prisma/migrations` (למשל דרך `prisma migrate dev` מול DB פיתוח פוסטגרסי, או
   כתיבת SQL ידנית + `prisma migrate resolve`). הפריסה מריצה `prisma migrate deploy`.
3. **ענף הפרודקשן:** `claude/busy-ptolemy-6u3vvd` (כך מוגדר ב-Vercel). כל push אליו
   עולה לאוויר. עבודה על פיצ'ר = ענף נפרד + מיזוג.
4. **במחשב הפיתוח הנוכחי:** Node מותקן נייד ב-`C:\Users\ASUS\tools\node-v22.13.1-win-x64`
   (מכונה זו חוסמת native addons לא חתומים — WDAC — לכן אין להוסיף תלויות עם
   בינארים native). ה-CLI של Vercel מאומת במחשב זה (session שמור) — אפשר לנהל
   משתני סביבה ופריסות ישירות.
5. **הסקרייפר הישן** (ליד מנג'ר) הוסר מהקוד ושמור בהיסטוריית גיט. הדאטה שלו:
   `prisma/legacy-leadmanager.db` + סקריפט `npm run import:legacy`.

---

## 4) מה נשאר — התוכנית עד פרודקשן מלא

> ההוראות המפורטות של כל חיבור — ב-[CONNECTIONS.md](CONNECTIONS.md) לפי מספרי הסעיפים.

### שלב 1 — השלמת חיבורי ליבה (בלי זה המערכת לא שלמה) · ~2 שעות

| # | משימה | הפניה | הערכה | תלות |
|---|---|---|---|---|
| 1.1 | **SMTP** — בחירת ספק מייל (Brevo/SendGrid/Gmail Workspace), 6 משתני סביבה, בדיקת תזכורת אמיתית | CONNECTIONS §3 | 30 דק׳ | חשבון מייל של המשרד |
| 1.2 | **Google OAuth** — פרויקט ב-Google Cloud, שני redirect URIs, משתני סביבה. פותח גם כניסת Google וגם את תשתית ה-SEO | CONNECTIONS §4 | 45 דק׳ | חשבון Google של המשרד |
| 1.3 | **תדירות תזכורות** — חשבון חינמי ב-cron-job.org שקורא ל-`/api/cron/reminders` כל 5 דק׳ עם `Authorization: Bearer <CRON_SECRET>` (חלופה: Vercel Pro) | CONNECTIONS §1 הערה | 15 דק׳ | — |

### שלב 2 — הפעלה תפעולית: הלקוח האמיתי הראשון · ~חצי יום

> בפרודקשן אין עדיין דאטה (נתוני הדמו הם מקומיים בלבד). זה החלק שבו המערכת
> מתחילה לעבוד באמת.

| # | משימה | הערות |
|---|---|---|
| 2.1 | יצירת הלקוח הראשון במערכת (צד משרד ⟵ לקוחות ⟵ לקוח חדש): סוג, לוגו, משתמשים (בעלים + סוכנים) | 10 דק׳ בתוך המערכת |
| 2.2 | יצירת מקורות קליטה ללקוח (הגדרות הלקוח ⟵ מקורות קליטה) והדבקת כתובות ה-webhook ב-Make/Zapier (פייסבוק לידים, פייקול) ובטפסי אלמנטור | CONNECTIONS §5. בדיקה: שליחת ליד אמיתי מהטופס |
| 2.3 | ייבוא ההיסטוריה מהמערכת הישנה (אם רלוונטי): `npm run import:legacy -- "<שם הלקוח>"` | ⚠️ להריץ מול פרודקשן: לייצר זמנית Prisma client פוסטגרסי ולכוון `DATABASE_URL` ל-Neon — ראו CONNECTIONS §10 |
| 2.4 | העלאת מסמכי הבסיס: הסכם חתום, קבלות החודש | דרך המערכת (מרכז מסמכים) |
| 2.5 | סקריפט הקבלות במחשב המשרד: העתקת `scripts/receipts-uploader.mjs` + ‏.env, תזמון יומי ב-Task Scheduler | CONNECTIONS §9 — הטוקן בצד השרת כבר מוגדר |
| 2.6 | **שער יציאה (מהתוכנית המקורית):** שבוע עבודה אמיתי של לקוח פיילוט אחד — לידים נכנסים, סטטוסים מתעדכנים, תזכורות מגיעות — לפני צירוף שאר הלקוחות | |

### שלב 3 — אינטגרציות מתקדמות (פר-לקוח, לפי צורך) · שעה–שעתיים כ"א

| # | משימה | הפניה | תלות |
|---|---|---|---|
| 3.1 | **SEO ללקוחות SEO:** הפעלת Search Console API + Analytics Data API ב-Google Cloud, חיבור OAuth פר-לקוח (טאב SEO), הגדרת siteUrl / Property ID (הגדרות הלקוח ⟵ אינטגרציות) | CONNECTIONS §6 | 1.2 |
| 3.2 | **Meta:** אפליקציה עסקית + System User token עם `ads_read`, הזנה בהגדרות הלקוח, "סנכרון עכשיו" — מפעיל וואטסאפים אוטומטיים + 2 המודעות החזקות | CONNECTIONS §7 | חשבון Business Manager |
| 3.3 | **SMS יוצא** (019 וכד׳) + **וואטסאפ יוצא** (WhatsApp Business Cloud API) — משתני סביבה בלבד; תזכורות/אוטומציות/תפוצה יתחילו להישלח בערוצים אלה | CONNECTIONS §8 | חשבון ספק |
| 3.4 | **פייקול:** תרחיש Make על שיחה חדשה ⟵ POST למקור מסוג "שיחות" (מספר, משך, הקלטה, סטטוס) | CONNECTIONS §5 | חשבון פייקול |

### שלב 4 — ליטוש פרודקשן (מומלץ, לא חוסם)

| # | משימה | הערות |
|---|---|---|
| 4.1 | **דומיין מותאם** (למשל `crm.הדומיין-שלכם.co.il`) — Vercel ⟵ Domains. לא לשכוח לעדכן את שני ה-redirect URIs של Google | משפר אמינות מול לקוחות |
| 4.2 | **ניטור:** ‏UptimeRobot חינמי על `/api/setup` (עונה תמיד JSON) + מבט תקופתי ב-Vercel Logs וביומן ההודעות במערכת | 15 דק׳ |
| 4.3 | **גיבויים:** Neon בחינמי שומר היסטוריה קצרה בלבד — לקבוע ייצוא שבועי (`pg_dump` מול `DATABASE_URL_UNPOOLED`) או שדרוג Neon | חשוב ככל שנכנס דאטה אמיתי |
| 4.4 | **הדרכה:** מסך "איך עובדים" קצר ללקוח ולסוכנים בעת צירוף | מהתוכנית המקורית (ו.4) |
| 4.5 | עתידי מתוכנן: מעבר ל-VPS (תוכנית ו.6 — המערכת בנויה ניידת), נראות ב-AI ל-SEO | לא לפני ששלב 3 יציב |

---

## 5) סיכום — "מה עושים מחר בבוקר"

1. ‏SMTP (1.1) ⟵ תזכורת מייל ראשונה אמיתית עובדת
2. ‏cron-job.org (1.3) ⟵ תזכורות בזמן אמת
3. ‏Google OAuth (1.2) ⟵ כניסה עם Google
4. יצירת הלקוח הראשון + חיבור טופס אמיתי (2.1–2.2) ⟵ **ליד אמיתי ראשון במערכת** 🎉
5. משם: פיילוט שבוע, ואז שאר הלקוחות והאינטגרציות לפי הצורך.
